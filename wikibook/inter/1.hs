import MyModule (coolFunction)
import MyOtherModule (CoolType(..)) -- ".." can be constructors
a = ConstructCoolType coolFunction
-- if you imported MyModule from wikibook/, you would use inter.MyModule